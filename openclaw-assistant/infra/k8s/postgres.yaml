apiVersion: apps/v1
kind: Deployment
metadata:
  name: ia-assistant-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ia-assistant-postgres
  template:
    metadata:
      labels:
        app: ia-assistant-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_PASSWORD
              value: openclaw
            - name: POSTGRES_USER
              value: openclaw
            - name: POSTGRES_DB
              value: openclaw
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: ia-assistant-postgres
  ports:
    - port: 5432
      targetPort: 5432
