apiVersion: apps/v1
kind: Deployment
metadata:
  name: ia-assistant-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ia-assistant-redis
  template:
    metadata:
      labels:
        app: ia-assistant-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: ia-assistant-redis
  ports:
    - port: 6379
      targetPort: 6379
